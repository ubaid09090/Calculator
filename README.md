# Calculator
This code creates a simple calculator with buttons for digits, operators, clear (C), and equals (=). Clicking buttons updates the display. The equals button uses JavaScript's `eval()` to calculate and show results. The calculator supports basic arithmetic operations like addition, subtraction, multiplication, and division.
